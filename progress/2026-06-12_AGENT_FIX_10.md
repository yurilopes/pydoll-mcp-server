# Progresso: FIX_10 - Locks e seguranca de element_screenshot

Plano atual: `plans/remediation/FIX_10_LOCKS_AND_ELEMENT_SCREENSHOT.md`.

## Tarefas concluidas

### T1-T3: Aplicar locks nas operacoes mutantes

`tab_operation_lock(tab_id)` aplicado em:
- `page.py`: `page_goto`, `page_reload`, `page_back`, `page_forward`
- `elements.py`: `element_click`, `element_type`, `element_fill`, `element_screenshot`
- `tabs.py`: `tab_close`, `tab_recover` (reload e recreate)
- `storage.py`: `storage_set`

### T4: Validar path em element_screenshot

- `validate_artifact_path()` extraido de `dom/tree.py` para `security/paths.py`.
- `page_screenshot` e `element_screenshot` usam o mesmo helper.
- Path relativo → `artifacts_dir`. Path absoluto → validado com `relative_to`. Proibido → `PERMISSION_DENIED`.

### T5: Testes de concorrencia

`tests/unit/test_concurrency.py`:
- `test_tab_operation_lock_serializes` - lock serializa mutacoes na mesma aba
- `test_different_tabs_independent_locks` - abas diferentes usam locks diferentes
- `test_page_goto_uses_lock` - verifica que `page_goto` contem `tab_operation_lock`
- `test_element_click_uses_lock` - verifica que `element_click` contem `tab_operation_lock`
- `test_tab_close_uses_lock` - verifica que `tab_close` contem `tab_operation_lock`
- `test_storage_set_uses_lock` - verifica que `storage_set` contem `tab_operation_lock`

### T6: Teste de element_screenshot com path proibido

`tests/unit/test_element_screenshot_security.py`:
- `test_rejects_absolute_forbidden_path` - path `C:\Windows\Temp\...` retorna `PERMISSION_DENIED`, `take_screenshot` nao e chamado
- `test_accepts_valid_relative_path` - path relativo e aceito
- `test_base64_no_path_accepted` - sem path retorna base64

## Gates finais

| Gate | Resultado |
|------|-----------|
| `pytest -q` | **118 passed** (+9 novos) |
| `ruff check .` | **All checks passed!** |
| `mypy src` | **Success** (33 source files) |
| `pytest -m browser_smoke -q` | **4 passed** |

## Arquivos alterados

- `src/pydoll_mcp_server/security/paths.py` - NOVO: helper `validate_artifact_path`
- `src/pydoll_mcp_server/dom/tree.py` - usa `security.paths.validate_artifact_path`, remove funcao inline
- `src/pydoll_mcp_server/tools/page.py` - locks aplicados + import `tab_operation_lock`
- `src/pydoll_mcp_server/tools/elements.py` - locks aplicados + path validation + import `validate_artifact_path`
- `src/pydoll_mcp_server/tools/tabs.py` - locks aplicados + import `tab_operation_lock`
- `src/pydoll_mcp_server/tools/storage.py` - lock aplicado + import `tab_operation_lock`
- `tests/unit/test_concurrency.py` - NOVO: 6 testes de concorrencia
- `tests/unit/test_element_screenshot_security.py` - NOVO: 3 testes de seguranca

## Riscos resolvidos do FIX_09

- Locks nao sao mais apenas definidos - sao usados pelas tools mutantes (comprovado por inspecao de source nos testes)
- `element_screenshot` nao aceita mais path arbitrario (comprovado por teste que verifica `PERMISSION_DENIED` e `take_screenshot` nao chamado)

## Bloqueios

Nenhum. FIX_10 concluido com todos os gates verdes.
