# Progresso: FIX_13 - Ajustes finais de aceitacao pos FIX_12

Plano atual: `plans/remediation/FIX_13_POST_FIX12_ACCEPTANCE.md`.

## Tarefas concluidas

### T1: Corrigir versao reportada
- Criado `src/pydoll_mcp_server/version.py` com `get_version()` usando `importlib.metadata.version("pydoll-mcp-server")` e fallback `0.1.0a1`
- `health_check`, `server_status` e `/health` usam `get_version()` (3 ocorrencias substituidas)
- Editable install reinstalado para metadata correta
- Teste: `assert result['version'] == '0.1.0a1'`

### T2: Integrar trace minimo com tools reais
- `TraceManager.add_event_to_active(client_id, event)` adicionado
- `TraceManager.get_active(client_id)` retorna trace rodando mais recente
- `diagnostics_snapshot` registra evento em trace ativo
- `network_enable`, `network_list`, `network_get_response` registram eventos via `_trace_event()` helper
- `_trace_event(client_id, tool, status, tab_id, error_code, summary)` nao registra secrets
- Testes: trace_start -> diagnostics_snapshot -> trace_get (2+ eventos)
- Testes: trace_start -> network_enable via _trace_event
- Testes: evento de erro com error_code

### T3: Limpar network_list
- `network_list` agora filtra logs sem `params.request.url` por padrao (list comprehension)
- Teste `test_network_list_filters_empty_urls`: 4 logs, 2 com URL, resultado 2 eventos
- Teste `test_network_list_calls_get_network_logs`: behavioral com fake tab, prova chamada real e normalizacao

### T4: Fortalecer testes comportamentais
- `test_cli_stdio_dispatches_without_config`: monkeypatch em `_run_stdio` e `uvicorn.run`, prova dispatch sem chamar HTTP
- `test_network_list_calls_get_network_logs`: fake tab com `get_network_logs` instrumentado
- Substituidos: `test_cli_dispatch_respects_transport` (source inspection), `test_network_list_uses_get_network_logs` (source inspection)
- Mantidos: `test_attach_no_endpoint_accepted` (inspect.signature - params sao contrato)

### T5: Atualizar documentacao
- `docs/release-checklist.md` atualizado com numeros finais (184 passed, 38 source files)
- Todos os itens marcados com estado real
- Progresso registrado

## Gates finais

| Gate | Resultado |
|------|-----------|
| `pytest -q` | **184 passed** (+3 from FIX_12) |
| `ruff check .` | **All checks passed!** |
| `mypy src` | **Success** (38 source files) |
| `pytest -m browser_smoke -q` | **9 passed** |
| `stdio` sem token | **OK** |
| `version` | **0.1.0a1** (health, status, /health) |

## Arquivos alterados

- `src/pydoll_mcp_server/version.py` - NOVO: helper centralizado
- `src/pydoll_mcp_server/server.py` - get_version(), trace em diagnostics_snapshot
- `src/pydoll_mcp_server/tools/inspection.py` - _trace_event, filter empty urls, trace em network_*
- `src/pydoll_mcp_server/diagnostics/trace.py` - get_active, add_event_to_active
- `tests/p2/test_p2_features.py` - behavioral stdio test, trace integration tests, version fix
- `tests/p2/test_network.py` - behavioral network mock test, empty filter test
- `docs/release-checklist.md` - numeros finais atualizados

## Bloqueios

Nenhum. FIX_13 concluido com todos os criterios atendidos.
