# Progresso: FIX_11 - Path traversal e locks cookies_set

Plano atual: `plans/remediation/FIX_11_PATH_TRAVERSAL_AND_COOKIE_LOCK.md`.

## Tarefas concluidas

### T1: Corrigir validate_artifact_path (path traversal)
- `security/paths.py`: path relativo agora resolve e verifica com `relative_to` que fica dentro de `artifacts_dir`
- Path absoluto: resolve both candidate e allowed dir com `strict=False` antes de `relative_to`
- `../escape.png` → `None` (bloqueado)
- `subdir/../../escape.png` → `None` (bloqueado)
- `subdir/screenshot.png` → aceito (dentro)
- `screenshot.png` → aceito (dentro)

### T2: Testar traversal relativo
`tests/unit/test_path_traversal.py` (9 testes):
- `test_accepts_relative_simple` - `screenshot.png` aceito
- `test_accepts_relative_subdir` - `subdir/screenshot.png` aceito
- `test_rejects_parent_traversal` - `../escape.png` rejeitado
- `test_rejects_nested_parent_traversal` - `subdir/../../escape.png` rejeitado
- `test_rejects_absolute_outside_allowlist` - path absoluto fora rejeitado
- `test_accepts_absolute_inside_allowlist` - path absoluto dentro aceito
- `test_accepts_absolute_in_downloads` - downloads dir aceito
- `test_accepts_absolute_in_tmp` - tmp dir aceito
- `test_rejects_absolute_near_prefix` - bypass por prefixo bloqueado

### T3-T4: Locks em cookies_set + testes
- `storage.py`: `cookies_set` agora usa `tab_operation_lock` com `tab_id` e `browser_operation_lock` com `browser_id`
- `tests/unit/test_concurrency.py`: `test_cookies_set_uses_tab_lock` e `test_cookies_set_uses_browser_lock`

## Gates finais

| Gate | Resultado |
|------|-----------|
| `pytest -q` | **129 passed** (+11 novos) |
| `ruff check .` | **All checks passed!** |
| `mypy src` | **Success** (33 source files) |
| `pytest -m browser_smoke -q` | **4 passed** |

## Arquivos alterados

- `src/pydoll_mcp_server/security/paths.py` - resolve + relative_to para path relativo e absoluto
- `src/pydoll_mcp_server/tools/storage.py` - locks em cookies_set + import browser_operation_lock
- `tests/unit/test_path_traversal.py` - NOVO: 9 testes de traversal
- `tests/unit/test_concurrency.py` - +2 testes de cookies_set

## Bloqueios

Nenhum. FIX_11 concluido com todos os gates verdes.
