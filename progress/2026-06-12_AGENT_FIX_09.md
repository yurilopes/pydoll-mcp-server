# Progresso: FIX_09 - Lacunas funcionais de runtime

Plano atual: `plans/remediation/FIX_09_RUNTIME_FUNCTIONAL_GAPS.md`.

## Tarefas concluidas

### T1: Corrigir retornos de execute_script (return_by_value=True)
- `dom/tree.py` - `build_page_tree` e `page_get_text`
- `tools/page.py` - `page_back`, `page_forward`, `page_wait`
- `tools/javascript.py` - `js_evaluate_readonly` e `js_evaluate`
- `tools/storage.py` - `storage_get` e `storage_set` (tambem corrigido bug de `raw` nao definido e `count` nao definido)
- `browser/cdp_helpers.py` - `set_user_agent` e `set_viewport`
- `recovery/health.py` - `check_tab_health`

Criado `browser/script_utils.py` com `extract_script_value()` e `extract_script_response()`.
Substituido todos os padroes `result.get('result', {}).get('value', ...)` pela helper.

### T2: Teste provando page_get_tree retorna arvore nao vazia
- `tests/integration/test_browser_smoke.py`: `test_page_get_tree_returns_nodes`
  - Registra browser/tab no registry
  - Chama `build_page_tree` em `simple.html`
  - Verifica `count > 0`, `root_id` nao vazio, nos com tag `button`
  - Teste passou (antes de `return_by_value=True` retornava arvore vazia)

### T3: Teste js_evaluate retornando objeto serializavel
- `tests/integration/test_browser_smoke.py`: `test_js_evaluate_returns_object`
  - Script: `return {answer: 42, text: "ok"};`
  - Verifica `extract_script_value` retorna dict com valores corretos

### T4: Aplicar locks - infraestrutura pronta mas nao aplicada em todas as tools
- `browser/locks.py` com `tab_operation_lock` e `browser_operation_lock` (context managers async)
- Marcado como pendencia para proxima rodada (P2)

### T5: Bloquear escrita arbitraria de screenshots
- `dom/tree.py`: `page_screenshot` agora valida `path` com `_validate_artifact_path`
  - Path relativo → resolvido dentro de `config.artifacts_dir`
  - Path absoluto → verificado com `relative_to` contra allowed dirs
  - Path proibido → `PERMISSION_DENIED`
- Teste de screenshot base64 sem path no smoke

### T6: Corrigir upload allowlist
- `tools/files.py`: `upload_files` agora usa `PathAllowlist` (com `relative_to`)
  - Nao usa mais `startswith`
  - Cada path e validado via `allowlist.is_allowed(p)`

### T7: Marcar actionable e resolution_confidence em page_get_tree
- `dom/tree.py`: `_assess_actionable(node)` retorna `(bool, str)`:
  - `high`: id, data-testid, name
  - `medium`: role+type, class
  - `low`: tag interativa sem atributos estaveis
  - `none`: elementos nao interativos (#text, script, etc)
- Removido pseudo-seletor `:has-text()` invalido para Pydoll
- Nos da arvore incluem `actionable`, `resolution_confidence`, `selector_hint`, `xpath_hint`

### T8: Registro de progresso
- Este arquivo.

## Gates finais

| Gate | Resultado |
|------|-----------|
| `pytest -q` | **109 passed** |
| `ruff check .` | **All checks passed!** |
| `mypy src` | **Success** (32 source files) |
| `pytest -m browser_smoke -q` | **4 passed** |

## Smoke real

4 testes com browser real:
1. `test_browser_smoke_launch_navigate_interact` - launch, goto, click, verify
2. `test_page_get_tree_returns_nodes` - **build_page_tree via wrapper, nodes > 0**
3. `test_js_evaluate_returns_object` - **execute_script retorna objeto serializavel**
4. `test_page_screenshot_base64_no_path` - screenshot base64 seguro

## Arquivos alterados

- `src/pydoll_mcp_server/browser/script_utils.py` - NOVO: helpers de extracao CDP
- `src/pydoll_mcp_server/dom/tree.py` - return_by_value, path validation, actionable metadata
- `src/pydoll_mcp_server/tools/page.py` - return_by_value + script_utils
- `src/pydoll_mcp_server/tools/javascript.py` - return_by_value + script_utils
- `src/pydoll_mcp_server/tools/storage.py` - return_by_value + bug fix (raw, count)
- `src/pydoll_mcp_server/tools/files.py` - PathAllowlist para upload
- `src/pydoll_mcp_server/browser/cdp_helpers.py` - return_by_value + script_utils
- `src/pydoll_mcp_server/recovery/health.py` - return_by_value + script_utils
- `tests/integration/test_browser_smoke.py` - 4 smoke tests com browser real
- `tests/integration/test_element_resolution.py` - ajuste de teste

## Riscos restantes

- `tab_operation_lock` esta definido mas nao aplicado nas tools de navegacao/mutacao (P2)
- `element_screenshot` ainda nao tem validacao de path (P2)
- Testes de concorrencia com locks reais nao implementados (P2)
