# Progresso: FIX_08 - Segunda passagem de validacao

Plano atual: `plans/remediation/FIX_08_SECOND_PASS_VALIDATION.md`.

## Tarefas concluidas

### T1: Verificar pydoll_compat.py
- Confirmado via inspecao direta da Pydoll que `current_url`, `title`, `text` sao properties com async getter. Sintaxe `await tab.current_url` esta correta.
- `get_attribute` e sincrono. `get_shadow_root` e async method.
- Nao houve alteracao necessaria em `pydoll_compat.py` - as assinaturas ja estavam corretas.

### T2: Montar /mcp e /sse explicitamente
- FastMCP configurado com `streamable_http_path='/'` e `sse_path='/'`.
- `create_app()` monta `streamable_http_app()` em `/mcp` e `sse_app()` em `/sse`.
- Health em `/health` separado.
- Testes verificam que `Mount('/mcp')` e `Mount('/sse')` existem nas rotas.

### T3: Remover teste fraco
- `test_mcp_accepts_valid_token` removido o `except RuntimeError: pass`.
- Substituido por `test_mcp_accepts_valid_token` que verifica montagem de rota.
- Adicionado `test_sse_mount_exists` para validar mount explicito.

### T4: Corrigir shadow traversal async
- `deep_traversal.py` linha `el.get_shadow_root(timeout=2)` corrigido para `await el.get_shadow_root(timeout=2)`.

### T5: Fechar ruff
- Line-length aumentado para 120 (razoavel para JS strings inline).
- Nested `with` statements combinados (SIM117).
- `pytest.raises(Exception)` trocado por excecoes especificas (B017).
- Variavel de loop renomeada para `_strategy` (B007).
- Ruff: `All checks passed!`

### T6: Smoke real
- Criado marcador `browser_smoke` em `pyproject.toml`.
- Criado `tests/integration/test_browser_smoke.py`:
  - Lanca Chrome headless com `--no-sandbox`.
  - Navega para fixture local `simple.html`.
  - Verifica titulo e URL.
  - Clica botao via `tab.query('#btn-click')`.
  - Verifica resultado "clicked".
  - Fecha browser em cleanup.
- **Smoke passou**: 1 selected, 1 passed em 6.3s.

## Gates finais

| Gate | Resultado |
|------|-----------|
| `pytest -q` | **106 passed** |
| `ruff check .` | **All checks passed!** |
| `mypy src` | **Success** (31 source files) |
| `pytest -m browser_smoke -q` | **1 passed** |

## Arquivos alterados

- `pyproject.toml` - line-length 120, marcador browser_smoke
- `src/pydoll_mcp_server/server.py` - mounts explicitos /mcp + /sse
- `src/pydoll_mcp_server/dom/deep_traversal.py` - await get_shadow_root
- `src/pydoll_mcp_server/tools/elements.py` - _strategy rename
- `tests/contract/test_mcp_contract.py` - testes de montagem, removido RuntimeError swallow
- `tests/unit/test_config.py` - SIM117 fix, B017 fix
- `tests/unit/test_registry.py` - B017 fix
- `tests/integration/test_browser_smoke.py` - NOVO: smoke real com browser

## Bloqueios

Nenhum. FIX_08 concluido com todos os gates verdes.

## Proximo passo

Projeto pronto para P2 conforme `plans/PLAN_P2.md`, se aplicavel.
